{
  "name": "mock",
  "version": "1.0.0",
  "description": "A simple mock to provide data",
  "main": "seeders.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "mock": "json-server --watch seeder.js --port 7000 --routes routes.json"
  },
  "author": "Nathan Tchinda",
  "license": "ISC",
  "dependencies": {
    "@faker-js/faker": "^8.0.2",
    "dayjs": "^1.11.9",
    "json-server": "^0.17.3"
  }
}